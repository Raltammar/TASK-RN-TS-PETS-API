import { View, Text } from "react-native";
import React from "react";

interface Pet {
  id: number;
  name: string;
  description: string;
  type: string;
  image: string;
  image2: string;
}

export default Pet;
